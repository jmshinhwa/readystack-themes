# Model Sunset Lint

![Model Sunset Lint - retired AI model IDs, dated](https://getreadystack.com/img/promo/sku33167_result_card.jpg)

Finds every model ID and API parameter in your code that a vendor has already switched off or has published a date for - 109 rules across OpenAI, Anthropic, Google and Azure, each carrying the real shutdown date and the exact replacement string.

## What it does for free

- Check the open file against all 109 rules - no key, no cap, no watermark
- Check only the lines you select
- Every hit carries the vendor, the real shutdown date and the replacement string
- error = already off today, warn = a date inside 120 days, info = later
- Read all 109 rules and their dates without running anything

## With a licence

- **Every file in the repository** — One pass over the whole workspace instead of the file you happen to have open.
- **Rewrite the retired IDs** — Replaces each retired ID with its published replacement, in place, instead of only naming it.
- **Dated inventory as CSV, JSON or HTML** — The migration list you hand to a team, with dates, not a panel you read once.
- **CI JSON so the build fails first** — A pipeline gate that catches a retired ID before the shutdown date does.

A US software engineer averages $70.92/hour (ZipRecruiter, August 2026). $29 is about 25 minutes of that, once.

[**Get the full version - $29**](https://getreadystack.com) - $29 once, one licence key per person or team seat, 7-day full refund.


## Install

```
ext install model-sunset-lint
```
