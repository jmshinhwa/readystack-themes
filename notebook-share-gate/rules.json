[
  {
    "id": "aws_access_key_id",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32 / Art. 33",
    "notify_hours": 72,
    "fine_cap": "EUR 20 million or 4 % of total worldwide annual turnover (GDPR Art. 83(5))",
    "pat": "\\bAKIA[0-9A-Z]{16}\\b",
    "msg": "\"{hit}\" is a live AWS access key id sitting in a saved cell output. Rotate it in IAM, then clear the output before the file leaves this machine."
  },
  {
    "id": "aws_secret_access_key",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "aws_secret_access_key['\"]?\\s*[:=]\\s*['\"]?[A-Za-z0-9/+=]{40}",
    "msg": "A 40-character AWS secret access key was printed into an output cell. Rotate the key pair, then clear the output."
  },
  {
    "id": "openai_style_api_key",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "\\bsk-[A-Za-z0-9_-]{20,}\\b",
    "msg": "\"{hit}\" is an API key of the sk- family printed into an output cell. Revoke it in the provider console, then clear the output."
  },
  {
    "id": "github_token",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "\\bgh[pousr]_[A-Za-z0-9]{30,}\\b",
    "msg": "\"{hit}\" is a GitHub token in a saved output. It grants whatever the token's scopes grant to anyone who opens this .ipynb. Revoke and clear."
  },
  {
    "id": "slack_token",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "\\bxox[abprs]-[A-Za-z0-9-]{10,}\\b",
    "msg": "\"{hit}\" is a Slack token in a saved output. Revoke it in the app's OAuth settings, then clear the output."
  },
  {
    "id": "bearer_jwt",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "\\beyJ[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{5,}",
    "msg": "A JWT was printed into an output cell. Anyone reading the notebook can replay it until it expires. Clear the output and shorten the token lifetime."
  },
  {
    "id": "db_url_with_password",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "\\b(?:postgres(?:ql)?|mysql|mariadb|mongodb(?:\\+srv)?|redis|mssql|clickhouse):\\/\\/[^\\s:@'\"]+:[^\\s@'\"]{3,}@[^\\s'\"]+",
    "msg": "\"{hit}\" is a database URL carrying its password in a saved output. Rotate the database credential and clear the output."
  },
  {
    "id": "private_key_block",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "-----BEGIN (?:RSA |EC |DSA |OPENSSH |PGP )?PRIVATE KEY-----",
    "msg": "A private key block was printed into an output cell. Treat the key as compromised, issue a new one, and clear the output."
  },
  {
    "id": "email_in_output",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 4(1) / Art. 33",
    "pat": "\\b[A-Za-z0-9._%+-]+@(?!example\\.(?:com|org|net)\\b)[A-Za-z0-9.-]+\\.(?!corp\\b|internal\\b|intranet\\b|local\\b|lan\\b)[A-Za-z]{2,}\\b",
    "msg": "\"{hit}\" is an email address held in a saved output — personal data under GDPR Art. 4(1). A notebook shared with it is a disclosure you have 72 hours to report under Art. 33. Clear the output or mask the column."
  },
  {
    "id": "iban_in_output",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 9 / Art. 33",
    "pat": "\\b(?:AT|BE|CH|CZ|DE|DK|ES|FI|FR|GB|IE|IT|LU|NL|NO|PL|PT|SE)[0-9]{2}[ ]?(?:[0-9A-Z]{4}[ ]?){3,7}[0-9A-Z]{1,4}\\b",
    "msg": "\"{hit}\" looks like an IBAN sitting in a saved output. Bank account numbers identify a person directly. Clear the output before the notebook is shared."
  },
  {
    "id": "card_pan_in_output",
    "sev": "error",
    "where": "output",
    "law": "PCI DSS 3.3 / GDPR Art. 33",
    "pat": "\\b(?:4[0-9]{3}|5[1-5][0-9]{2})[ -]?[0-9]{4}[ -]?[0-9]{4}[ -]?[0-9]{4}\\b",
    "msg": "\"{hit}\" has the shape of a full card number in a saved output. PCI DSS 3.3 allows at most the first six and last four digits to be displayed. Clear the output."
  },
  {
    "id": "internal_hostname",
    "sev": "warn",
    "where": "output",
    "law": "NIS2 Art. 21 (risk-management measures)",
    "pat": "\\b[a-z0-9][a-z0-9-]{1,40}\\.(?:internal|intranet|corp|lan|local)\\b",
    "msg": "\"{hit}\" is an internal hostname in a saved output. It maps your private network for whoever reads the notebook. Clear the output."
  },
  {
    "id": "private_ip_in_output",
    "sev": "warn",
    "where": "output",
    "law": "NIS2 Art. 21 (risk-management measures)",
    "pat": "\\b(?:10\\.(?:[0-9]{1,3}\\.){2}[0-9]{1,3}|192\\.168\\.[0-9]{1,3}\\.[0-9]{1,3}|172\\.(?:1[6-9]|2[0-9]|3[01])\\.[0-9]{1,3}\\.[0-9]{1,3})\\b",
    "msg": "\"{hit}\" is a private network address in a saved output. Together with a hostname it is a map of your internal estate. Clear the output."
  },
  {
    "id": "local_user_path",
    "sev": "warn",
    "where": "output",
    "law": "GDPR Art. 4(1)",
    "pat": "(?:\\/(?:home|Users)\\/[A-Za-z0-9._-]{2,}\\/)|(?:[Cc]:\\\\Users\\\\[A-Za-z0-9._-]{2,}\\\\)",
    "msg": "\"{hit}\" is a traceback path carrying a named person's home directory. The employee name travels with the notebook. Clear the output."
  },
  {
    "id": "env_secret_dump",
    "sev": "error",
    "where": "output",
    "law": "GDPR Art. 32",
    "pat": "'[A-Z][A-Z0-9_]{2,}_(?:TOKEN|SECRET|PASSWORD|PASSWD|API_KEY|APIKEY|CREDENTIALS)':\\s*'[^']{4,}'",
    "msg": "\"{hit}\" is an environment variable printed with its value — the classic os.environ dump. Rotate what it names and clear the output."
  },
  {
    "id": "hardcoded_secret_in_source",
    "sev": "warn",
    "where": "source",
    "law": "GDPR Art. 32",
    "pat": "\\b(?:api_key|apikey|password|passwd|secret|token|access_key)\\s*=\\s*[\"'][^\"'\\s]{8,}[\"']",
    "msg": "\"{hit}\" hard-codes a credential in the cell source, so clearing outputs will not remove it. Move it to an environment variable or a .env file that is git-ignored."
  },
  {
    "id": "execution_order_regressed",
    "sev": "warn",
    "where": "struct",
    "pat": "",
    "law": "reproducibility",
    "msg": "Cell execution counts run out of order, so the saved outputs were not produced by a top-to-bottom run. Whoever audits this notebook cannot reproduce the numbers it shows. Restart and run all before sharing."
  },
  {
    "id": "widget_state_saved",
    "sev": "warn",
    "where": "struct",
    "pat": "",
    "law": "GDPR Art. 5(1)(c)",
    "msg": "The notebook carries saved ipywidgets state in its metadata. Widget state keeps a copy of values the visible outputs no longer show. Clear widget state before sharing."
  }
]
