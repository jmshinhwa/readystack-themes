MCP Config Guard - agent config lint — Licence

Free scope
  Check the MCP config file you have open against every rule - line number, why it is wrong, and the exact line that replaces it. is free for any use, personal or commercial, for as long as you keep the
  extension installed, and the online check at the product page is free without registration.

Paid scope
  Scan every MCP config in the workspace and in the client config folders at once, export the dated audit as CSV/JSON/HTML, and emit machine-readable output a CI step can fail on. is the paid part. It asks for a licence key issued at purchase (one seat, one key;
  7-day full refund, no questions). The key is checked against the payment provider and
  cached locally for 30 days so it keeps working offline.

Redistribution
  You may not resell, re-host or bundle this extension or its rule set. The rule set and the
  engine are provided as-is; check results are advisory and do not constitute legal advice.

(c) ReadyStack — https://getreadystack.com
