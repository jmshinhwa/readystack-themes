# MCP 2026 Migration Lint

![MCP 2026 Migration Lint](https://getreadystack.com/img/promo/sku35254_result_card.jpg)

The 2026-07-28 revision removed the initialize handshake, sessions, ping and four more RPCs. Your assistant was trained before that and still writes them.

## What it does for free

- All 27 rules on the file you have open, with line numbers and the fix
- Check just the lines you selected
- Read every rule, with the removal it maps to

## With a licence

- **The whole repository** — Every mcp.json, claude_desktop_config.json and server source file in the workspace in one pass, instead of one open file at a time.
- **Migration report as a file** — Writes the findings to CSV, JSON or HTML in the workspace, so the migration can be handed to someone else or attached to a ticket.
- **CI output that fails the build** — Machine-readable output a pipeline reads, so a removed RPC cannot be merged again after you have cleaned it out.

Full workspace sweep and report: free for 7 days from your first sweep, then a licence key.

Freelance senior software engineers publish an average of $101/hour (contractrates.fyi, 2026, 584 submissions).

[**Get the full version - $29 once**](https://getreadystack.com)


## Install

```
ext install mcp-2026-migration-lint
```
