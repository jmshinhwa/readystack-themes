<!-- android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:resizeableActivity="false"
        android:theme="@style/AppTheme">

        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_OPT_OUT_EDGE_TO_EDGE"
            android:value="true" />
        <meta-data
            android:name="windowOptOutEdgeToEdgeEnforcement"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".sync.TripTrackerService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <receiver android:name=".alarm.ReminderReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>

// android/app/build.gradle
buildscript {
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.4"
    }
}

android {
    namespace "com.acme.fieldnotes"
    compileSdkVersion 34
    ndkVersion "25.1.8937393"

    defaultConfig {
        applicationId "com.acme.fieldnotes"
        minSdkVersion 24
        targetSdkVersion 34
        versionCode 812
        versionName "4.3.1"
    }

    packagingOptions {
        jniLibs {
            useLegacyPackaging true
        }
    }
}

dependencies {
    implementation "com.android.billingclient:billing:6.1.0"
    implementation "androidx.core:core-ktx:1.12.0"
}
