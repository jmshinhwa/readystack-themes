Model Card Lint - EU GPAI (AI Act Art. 53) — Licence

Free scope
  Lint the model card you have open against every rule, as often as you like - line number, the rule, the article it comes from, and the line to write instead. is free for any use, personal or commercial, for as long as you keep the
  extension installed, and the online check at the product page is free without registration.

Paid scope
  Lint every model card in the repository in one pass and export the dated audit as a file you keep (Markdown + JSON a CI step can fail on). is the paid part. It asks for a licence key issued at purchase (one seat, one key;
  7-day full refund, no questions). The key is checked against the payment provider and
  cached locally for 30 days so it keeps working offline.

Redistribution
  You may not resell, re-host or bundle this extension or its rule set. The rule set and the
  engine are provided as-is; check results are advisory and do not constitute legal advice.

(c) ReadyStack — https://getreadystack.com
